import React, { Component } from 'react'
import { connect } from 'react-redux'

class Post extends Component {
  render() {
    const {params} = this.props
  }
}

export default () => <div>Post will live here</div>
